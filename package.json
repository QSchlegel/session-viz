{
  "name": "session-viz-plugin",
  "private": true,
  "version": "0.19.0",
  "description": "Local-first delivery assurance for Claude Code, Codex and Cursor",
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "@types/node": "^22.10.2"
  }
}
